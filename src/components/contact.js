/*
    this module contains all the logic pertaining to the contact card on the UI,
    which essentially is the saved Profile object in localStorage (soon to be Firebase).
    here, the CRUD operations will be integrated.
*/
